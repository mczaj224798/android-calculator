package com.example.calculator;

public enum CalcButton {
    PLUS, MINUS, DIVIDE, MULTIPLY, EQUALS, CE, AC, COMA, SAVE, MEM1, MEM2, BACK, INIT, NUMBER, //basic
    SIN, COS, TAN, LOG, LN, X2, XY, PERCENT, SQRT // advanced
}
